---
description: Schedule calculation constants — must match scripts/sprintkit/schedule.py
alwaysApply: true
---

# Schedule Engine Constants

Do not recalculate dates in prose; run the pipeline (`scripts/sprint_report.py`; engine in `scripts/sprintkit/schedule.py`).

| Rule | Value |
|------|-------|
| Hours per day | 8 |
| Working days | Mon–Fri |
| Duration | `ceil(estimateSeconds / (hoursPerDay * 3600))`, min 1 if estimate > 0 |
| Priority sort | Epic priority → story rank → created date |
| Task priority | Inherit from parent Epic |
| BE → Web/Mobile verification | After BE Development ends: if `remainingDays < requiredDays`, extend Web/Mobile Development end by `requiredDays - remainingDays`. `requiredDays = min(15% × FE dev hours, 2 days × 6h) ÷ 6h` (calendar days, not split by dev resources); `remainingDays` = signed business days from BE end to FE end. Jira start dates unchanged. |

Effort source: sum `timeoriginalestimate` on Tasks/Sub-tasks per Story, assignee-wise. **Bug** issues use `timespent` when Original Estimate is empty (see `effective_estimate_seconds` in `scripts/sprintkit/jira_model.py`).

Status classification (post-engine):

- **on_track**: due ≤ sprint end, not blocked
- **at_risk**: due within 2 business days of sprint end, or missing estimates on story
- **delayed**: due > sprint end, or overdue and not Done
- **blocked**: Jira blocked status, blocker link, or impediment label
